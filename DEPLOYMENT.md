# 🚀 Інструкції для деплою Help Krok Platform

## 📋 Передумови

- GitHub репозиторій (проект має бути завантажений на GitHub)
- Vercel акаунт (безкоштовний) або інший хостинг
- Supabase проект (безкоштовний tier доступний)
- Налаштована база даних

## 🔧 Крок 1: Підготовка проекту

### 1.1 Перевірка Git репозиторію
Переконайтеся, що ваш проект завантажений на GitHub:

```bash
# Перевірте статус
git status

# Якщо є незбережені зміни, додайте їх
git add .
git commit -m "Prepare for deployment"

# Завантажте на GitHub (якщо ще не завантажено)
git push origin main
```

### 1.2 Змінні середовища
Створіть файл `.env.local` на основі `env.example` (для локального тестування):

```bash
cp env.example .env.local
```

**ВАЖЛИВО**: Файл `.env.local` не повинен бути завантажений на GitHub (має бути в `.gitignore`)

## 🌐 Крок 2: Деплой на Vercel (Рекомендовано)

### 2.1 Підключення до Vercel

1. Перейдіть на [vercel.com](https://vercel.com)
2. Увійдіть через GitHub (натисніть "Continue with GitHub")
3. Натисніть "Add New..." → "Project"
4. Виберіть ваш репозиторій `help-krok-platform`
5. Натисніть "Import"

**Альтернатива через CLI:**
```bash
# Встановіть Vercel CLI
npm i -g vercel

# Увійдіть
vercel login

# Деплой
vercel

# Для production
vercel --prod
```

### 2.2 Налаштування змінних середовища

**ВАЖЛИВО**: Проект налаштований для PostgreSQL (Supabase), не SQLite!

В Vercel Dashboard:

1. Після імпорту проекту, натисніть "Environment Variables"
2. Або перейдіть до Settings → Environment Variables
3. Додайте наступні змінні (для Production, Preview та Development):

#### Обов'язкові змінні:
```
DATABASE_URL=postgresql://postgres.nttyhnbjlsnrxbcoxglw:your_password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
NEXTAUTH_URL=https://your-app-name.vercel.app
NEXTAUTH_SECRET=your-secret-key-here
NEXT_PUBLIC_SUPABASE_URL=https://nttyhnbjlsnrxbcoxglw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im50dHlobmJqbHNucnhiY294Z2x3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTc5NDUyMzEsImV4cCI6MjA3MzUyMTIzMX0.l6ieAE6s5CPxJ8Mw39MYLXAIvhwhFZzwFEea5z1EA-8
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
NEXT_PUBLIC_APP_URL=https://your-app-name.vercel.app
```

#### Опціональні змінні (для повної функціональності):
```
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-email@gmail.com
EMAIL_SERVER_PASSWORD=your-app-password
EMAIL_FROM=noreply@helpkrok.com
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_BOT_USERNAME=your_bot_username
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret
```

### 2.3 Налаштування Build Settings

В Vercel Dashboard:

1. Перейдіть до Settings → General
2. Framework Preset: **Next.js** (визначиться автоматично)
3. Build Command: `npm run build` (за замовчуванням)
4. Output Directory: `.next` (за замовчуванням)
5. Install Command: `npm install` (за замовчуванням)
6. Root Directory: `.` (за замовчуванням)

**Примітка**: Vercel автоматично визначає Next.js проекти, тому більшість налаштувань вже правильні.

### 2.4 Деплой

1. Після додавання всіх змінних середовища, натисніть "Deploy"
2. Дочекайтеся завершення білду (зазвичай 2-5 хвилин)
3. Після успішного деплою ви отримаєте URL типу: `https://help-krok-platform.vercel.app`
4. Перевірте, що сайт працює, відкривши URL

**Автоматичний деплой**: Після першого деплою, кожен push до `main` гілки автоматично запускає новий деплой!

## 🗄️ Крок 3: Налаштування бази даних

### 3.1 Supabase налаштування

1. Перейдіть до вашого Supabase проекту
2. Переконайтеся, що всі таблиці створені
3. Перевірте RLS (Row Level Security) політики
4. Налаштуйте аутентифікацію

### 3.2 Створення адміністратора

Після деплою створіть адміністраторський акаунт через API або Supabase Dashboard.

## 🔍 Крок 4: Перевірка та тестування

### 4.1 Базова перевірка

1. Відкрийте сайт
2. Перевірте реєстрацію/логін
3. Протестуйте основні функції
4. Перевірте мобільну версію

### 4.2 Налаштування домену (опціонально)

1. В Vercel Dashboard → Settings → Domains
2. Додайте ваш домен
3. Налаштуйте DNS записи

## 🚨 Вирішення проблем

### Проблема: Build fails
- Перевірте всі змінні середовища
- Переконайтеся, що всі залежності встановлені
- Перевірте логи білду

### Проблема: Database connection error
- Перевірте DATABASE_URL
- Переконайтеся, що Supabase проект активний
- Перевірте RLS політики

### Проблема: Authentication не працює
- Перевірте NEXTAUTH_URL та NEXTAUTH_SECRET
- Переконайтеся, що Supabase аутентифікація налаштована

## 📞 Підтримка

При виникненні проблем:
1. Перевірте логи в Vercel Dashboard
2. Перевірте Supabase Dashboard
3. Переконайтеся, що всі змінні середовища налаштовані правильно

## 🎉 Готово!

Ваш сайт тепер доступний за адресою: `https://your-app-name.vercel.app`

---

## 🌍 Альтернативні варіанти деплою

### Варіант 1: Netlify

1. Перейдіть на [netlify.com](https://netlify.com)
2. Увійдіть через GitHub
3. Натисніть "Add new site" → "Import an existing project"
4. Виберіть репозиторій
5. Налаштування:
   - Build command: `npm run build`
   - Publish directory: `.next`
   - Environment variables: додайте всі змінні з `env.example`

### Варіант 2: Railway

1. Перейдіть на [railway.app](https://railway.app)
2. Увійдіть через GitHub
3. Натисніть "New Project" → "Deploy from GitHub repo"
4. Виберіть репозиторій
5. Додайте змінні середовища в Settings → Variables

### Варіант 3: Render

1. Перейдіть на [render.com](https://render.com)
2. Увійдіть через GitHub
3. Натисніть "New" → "Web Service"
4. Виберіть репозиторій
5. Налаштування:
   - Build Command: `npm run build`
   - Start Command: `npm start`
   - Environment: `Node`

### Варіант 4: Self-hosted (VPS)

Якщо у вас є VPS (DigitalOcean, AWS, тощо):

```bash
# На сервері
git clone https://github.com/your-username/help-krok-platform.git
cd help-krok-platform
npm install
npm run build

# Створіть .env файл
nano .env
# Додайте всі змінні середовища

# Запустіть з PM2
npm install -g pm2
pm2 start npm --name "help-krok" -- start
pm2 save
pm2 startup
```

---

## 🔐 Генерація NEXTAUTH_SECRET

Для безпеки, згенеруйте унікальний `NEXTAUTH_SECRET`:

```bash
# В терміналі
openssl rand -base64 32
```

Або використайте онлайн генератор: https://generate-secret.vercel.app/32

---

## 📝 Чеклист перед деплоєм

- [ ] Проект завантажений на GitHub
- [ ] Всі змінні середовища підготовлені
- [ ] Supabase проект налаштований
- [ ] База даних містить необхідні таблиці
- [ ] Тестовий запуск локально (`npm run build` працює)
- [ ] `.env.local` в `.gitignore` (не завантажується на GitHub)

---

**Примітка**: Цей проект використовує Next.js 15, React 19, Supabase та інші сучасні технології. Переконайтеся, що всі залежності сумісні.
