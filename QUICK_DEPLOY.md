# ⚡ Швидкий гайд: Як закинути сайт на хостинг

## 🎯 Найпростіший спосіб: Vercel (5 хвилин)

### Крок 1: Завантажте проект на GitHub
```bash
# Перевірте статус
git status

# Додайте всі зміни
git add .
git commit -m "Ready for deployment"

# Завантажте на GitHub
git push origin main
```

### Крок 2: Деплой на Vercel

1. **Перейдіть на [vercel.com](https://vercel.com)**
2. **Увійдіть через GitHub** (натисніть "Continue with GitHub")
3. **Натисніть "Add New..." → "Project"**
4. **Виберіть репозиторій** `help-krok-platform`
5. **Натисніть "Import"**

### Крок 3: Додайте змінні середовища

Після імпорту проекту, натисніть **"Environment Variables"** та додайте:

#### Обов'язкові (мінімум для роботи):
```
DATABASE_URL=ваш_supabase_connection_string
NEXTAUTH_URL=https://ваш-проект.vercel.app
NEXTAUTH_SECRET=згенеруйте_через_openssl_rand_-base64_32
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app
```

**Де знайти Supabase дані:**
- Перейдіть на [supabase.com](https://supabase.com)
- Відкрийте ваш проект
- Settings → API → скопіюйте URL та ключі

**Як згенерувати NEXTAUTH_SECRET:**
```bash
openssl rand -base64 32
```

### Крок 4: Деплой!

1. Натисніть **"Deploy"**
2. Дочекайтеся 2-5 хвилин
3. Готово! 🎉

Ваш сайт буде доступний за адресою: `https://ваш-проект.vercel.app`

---

## 🚀 Альтернативи (якщо Vercel не підходить)

### Netlify
1. [netlify.com](https://netlify.com) → Увійдіть через GitHub
2. "Add new site" → "Import an existing project"
3. Виберіть репозиторій
4. Build command: `npm run build`
5. Publish directory: `.next`
6. Додайте змінні середовища

### Railway
1. [railway.app](https://railway.app) → Увійдіть через GitHub
2. "New Project" → "Deploy from GitHub repo"
3. Виберіть репозиторій
4. Додайте змінні середовища в Settings → Variables

### Render
1. [render.com](https://render.com) → Увійдіть через GitHub
2. "New" → "Web Service"
3. Виберіть репозиторій
4. Build Command: `npm run build`
5. Start Command: `npm start`

---

## ⚠️ Важливо перед деплоєм

1. ✅ Проект на GitHub
2. ✅ Supabase проект створений
3. ✅ База даних налаштована
4. ✅ Локально `npm run build` працює без помилок
5. ✅ `.env.local` НЕ завантажений на GitHub (перевірте `.gitignore`)

---

## 🆘 Проблеми?

**Build fails?**
- Перевірте всі змінні середовища
- Переконайтеся, що `npm run build` працює локально

**Database connection error?**
- Перевірте `DATABASE_URL` в Supabase
- Переконайтеся, що Supabase проект активний

**Site не працює?**
- Перевірте логи в Dashboard хостингу
- Перевірте `NEXTAUTH_URL` (має бути URL вашого сайту)

---

## 📚 Детальні інструкції

Для більш детальних інструкцій дивіться [DEPLOYMENT.md](./DEPLOYMENT.md)

